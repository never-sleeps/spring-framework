spring:
  data:
    mongodb:
      database: library
      port: 27017
      host: localhost
logging:
  level:
    ROOT: INFO

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 3000